{
  "extends": "../../tsconfig.json", // 继承根配置
  "compilerOptions": {
    "outDir": "dist" // 输出目录
  },
  "exclude": ["dist", "node_modules"],
  "include": ["**/*.ts", "**/*.vue", "../../shims-vue.d.ts"] // 包含 src 目录
}
